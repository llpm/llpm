#include "block.hpp"

namespace llpm {


#if 0
bool Block::refine(std::vector<Block*>& blocks) {
    if (!this->refinable())
        return false;
    assert(false && "Not yet implemented!");
}
#endif

} // namespace llpm
